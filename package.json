{
  "name": "uzumaki",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "pnpm --filter playground start",
    "build:core": "cargo build --release -p uzumaki",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
